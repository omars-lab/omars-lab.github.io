/**
 * Sidebar for the 📘 Handbook docs instance (internal id: 'legend', routeBasePath: /handbook).
 * Autogenerated from docs/handbook/: the handbook for navigating the blog. It holds the
 * durable/temporal explainer + post-kind emoji, the glossary, all the terminology pages, and
 * the component reference. Its own instance + sidebar, like Craft/Journey, so the two never mix.
 * (The internal plugin id stays 'legend' even though the route is /handbook, the same way the
 * Journey instance kept id 'self'; only the route + label are reader-facing.)
 */
module.exports = {
  legendSidebar: [{type: 'autogenerated', dirName: '.'}],
};
