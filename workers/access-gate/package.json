{
  "name": "access-gate",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Worker: LinkedIn identity (/api/me) + StatiCrypt key-vend (/api/unlock-key), gated by Cloudflare Access.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.84.0"
  }
}
